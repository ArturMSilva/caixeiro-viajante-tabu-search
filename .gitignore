# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Arquivos temporários da geração do GIF ---
# Os frames intermediários são apagados automaticamente por visualizacao.py,
# mas ficam ignorados aqui como segurança (ex.: execução interrompida).
frames_temp/

# --- Streamlit ---
.streamlit/secrets.toml

# --- Arquivo de apoio (não faz parte da entrega) ---
prompt-claude-code-tsp.md

# --- Editores / SO ---
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store

# OBSERVAÇÃO: as saídas visuais de exemplo (rota_inicial.png, rota_final.png,
# convergencia.png e evolucao.gif) são versionadas de propósito, para que o
# README exiba os resultados direto no GitHub sem exigir que o leitor execute
# o projeto.
